---
id: 002
date: 2026-07-15
package-under-test: 26.7.0
type: maintenance
---

# Target GroupDocs.Total.Mcp 26.7.0

## What changed

- Bumped the package-under-test from `26.5.0` → `26.7.0` everywhere: default
  `<McpPackageVersion>` in `Directory.Build.props`, the `integration.yml`
  workflow input default + fallback, all `how-to/*` and `examples/*` version
  pins, `README.md`, `AGENTS.md`, `llms.txt`, and the `docker-scripts/*`.
- Added **how-to/07-use-with-cursor.md** + **examples/cursor-mcp.json** —
  Docker-first Cursor setup (Total's nupkg exceeds NuGet's 250 MB limit), with
  the `dnx` and Windows `dotnet.exe`-cached-DLL fallbacks documented for parity.
- Fixed Signature-clone residue in `how-to/README.md` (the "Common context"
  section claimed the server exposed only `sign`/`verify` — it exposes 38 tools
  across 10 product domains).

## Why

Keeps the integration suite pinned to the current published MCP release and adds
the standard Cursor onboarding guide used across the GroupDocs MCP repos.

## Migration / impact

`ToolDiscoveryTests` still asserts the full **38-tool** catalog; no tool-surface
change. The suite exercises the Docker image / `dnx` package at `26.7.0`.
